pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: jenkins-nodejs
        stages:
          - name: Build
            steps:
              - command: /bin/bash
                args:
                  - npm
                  - install
                    
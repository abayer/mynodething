pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: nodejs
        stages:
          - name: Build
            steps:
              - command: /bin/bash
                args:
                  - npm
                  - install
                    